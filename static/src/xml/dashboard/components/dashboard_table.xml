<?xml version="1.0" encoding="utf-8"?>
<templates xml:space="preserve">
    <t t-name="shell_dashboard.Table" owl="1">
        <div class="card">
            <!-- Table Header -->
            <div class="card-header d-flex justify-content-between align-items-center p-2 border-bottom">
                <h6 t-esc="props.block.name" class="table-title mb-0 fw-bold" />
                <div class="table-actions">
                    <button t-on-click="configureBlock" class="btn btn-sm btn-outline-secondary">
                        <i class="fa fa-cog" />
                    </button>
                </div>
            </div>
            <!-- Table Body -->
            <div class="card-body container">
                <div class="table-responsive" style="max-height: 300px;">
                    <table class="table table-striped table-hover table-sm mb-0">
                        <thead class="table-light sticky-top">
                            <tr>
                                <t t-foreach="props.block.data.columns" t-as="column" t-key="column_index">
                                    <th t-esc="column" class="text-nowrap" />
                                </t>
                            </tr>
                        </thead>
                        <tbody>
                            <t t-foreach="state.rows" t-as="row" t-key="row.id">
                                <tr t-on-click="(ev) => openRecord(row)">
                                    <t t-foreach="props.block.data.columns" t-as="colName" t-key="colName_index">
                                        <td class="text-nowrap">
                                            <t t-esc="formatCellValue(row, colName)" />
                                        </td>
                                    </t>
                                </tr>
                            </t>
                        </tbody>
                    </table>
                </div>
                <!-- Empty State -->
                <t t-if="props.block.data.rows.length === 0">
                    <div class="text-center py-5 text-muted">
                        <i class="fa fa-database fa-2x mb-2" />
                        <p class="mb-0">No data available</p>
                    </div>
                </t>
            </div>
            <!-- Table Footer -->
            <div class="table-footer px-3 py-2 border-top">
                <div class="d-flex justify-content-between align-items-center small">
                    <div>
            Showing
                        <t t-esc="state.rows.length" />
            of
                        <t t-esc="state.total" />
            records
                    </div>
                    <div t-if="state.paginationEnabled">
                        <t t-if="state.loading">
                            <i class="fa fa-spinner fa-spin me-1" /> Loading...
                        </t>
                        <t t-else="">
                            <ul class="pagination pagination-sm mb-0">
                                <!-- Previous -->
                                <li t-att-class="'page-item ' + (state.currentPage === 1 ? 'disabled' : '')">
                                    <a class="page-link" href="#" t-on-click.prevent="() => goToPage(state.currentPage - 1)">
                                        <i class="fa fa-chevron-left" />
                                    </a>
                                </li>
                                <!-- Page numbers -->
                                <t t-foreach="visiblePages" t-as="page" t-key="page">
                                    <li t-att-class="'page-item ' + (state.currentPage === page ? 'active' : '')">
                                        <a class="page-link" href="#" t-on-click.prevent="() => goToPage(page)">
                                            <t t-esc="page" />
                                        </a>
                                    </li>
                                </t>
                                <!-- Next -->
                                <li t-att-class="'page-item ' + (state.currentPage === totalPages ? 'disabled' : '')">
                                    <a class="page-link" href="#" t-on-click.prevent="() => goToPage(state.currentPage + 1)">
                                        <i class="fa fa-chevron-right" />
                                    </a>
                                </li>
                            </ul>
                        </t>
                    </div>
                </div>
            </div>
        </div>
    </t>
</templates>